super注意点:
    1.super调用父类的构造方法,必须在构造方法的第一个
    2.super 必须在子类的方法或构造器中
    3.super和this不能同时调用构造方法
VS this:
    代表不同的对象:
        this:代表调用者本身
        super:代表父类的对象应用
    前提:
        this:没有继承也可由使用
        super:只能在继承条件下使用
    构造方法:
        this()本类的构造
        super()父类的构造